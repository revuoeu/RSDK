using System.Text.Json;

namespace {{ProjectName}}.Test;

// ── Plain model tests (no DI, no Blazor) ─────────────────────────────────────
// These compile and run without any additional setup.
// Replace the example with tests for your own models.

public class UnitTest1
{
    [Fact]
    public void PlaceholderTest_Passes()
    {
        // Verifies the test project is wired correctly.
        // Replace with a real test once you have a model to exercise.
        Assert.True(true);
    }
}
